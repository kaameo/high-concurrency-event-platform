kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: event-platform
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
      - containerPort: 30000
        hostPort: 30300
        protocol: TCP
  - role: worker
  - role: worker
